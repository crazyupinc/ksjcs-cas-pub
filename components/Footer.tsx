import type { NextPage } from "next";
import { memo } from "react";

const Footer: NextPage = memo(() => {
  return (
    <div className="bg-text-colors-dark-gray max-w-full h-[102px] flex flex-row items-center justify-center p-2.5 box-border text-center text-17xl text-text-colors-amber font-headings-headling-2 self-stretch">
      <div className="relative leading-[40px]">TODO: FOOTER</div>
    </div>
  );
});

export default Footer;
